# Public URLs — set these to your actual domains after adding them in Coolify
OUTLINE_URL=https://outline.yourdomain.com
AUTHENTIK_URL=https://auth.yourdomain.com

# Get this from Authentik's admin UI after first setup (Applications > Providers)
OIDC_CLIENT_SECRET=changeme-after-authentik-setup
